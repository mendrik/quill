slick {
  dbs {
    default {
      profile = "slick.jdbc.PostgresProfile$"
      db {
        driver = org.postgresql.Driver
        url = "jdbc:postgresql://localhost/quill"
        user = "quill"
        password = "quill42"
        numThreads = 30
        queueSize = 20
      }
      hikaricp {
        maximumPoolSize = 30
        minimumIdle = 30
      }
    }
  }
}
