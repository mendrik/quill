sidebar {

    .level {
        padding: 5px;

        .level-left,
        .level-right {
            > .button,
            > .toggler {
                margin-right: 5px;
            }
        }
    }

    aside {
        padding: var(--pad);
        width: 100%;
    }

    .tree-view {
        padding: 0;
    }

    .create-node {
        padding: 5px;
    }
}

