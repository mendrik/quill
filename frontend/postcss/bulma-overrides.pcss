.input.is-warning,
.textarea.is-warning {
    border-color: #dbdbdb;

    + .icon i:before {
        color: var(--red);
    }
}

.navbar-menu.is-active {
    position: absolute;
    width: 100%;
}

.menu-label {
    white-space: nowrap;
}
